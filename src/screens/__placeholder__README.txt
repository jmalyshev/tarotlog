This folder contains placeholder components for the Expo scaffold.

Next steps you can take after installing dependencies:
- Replace the placeholder drag logic with Reanimated + gesture-handler for smoother gestures.
- Wire Supabase auth and client, and replace sample card catalog with the real one.
- Add local persistent store (SQLite or WatermelonDB) and implement sync.
